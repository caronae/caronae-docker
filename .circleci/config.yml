version: 2

references:
  defaults: &defaults
    machine:
      enabled: true

  restore_repo: &restore_repo
    restore_cache:
      keys:
        - v1-repo-{{ .Branch }}-{{ .Revision }}
        - v1-repo-{{ .Branch }}
        - v1-repo

jobs:
  validate-config:
    <<: *defaults
    steps:
      - checkout
      - run: docker-compose -f docker-compose.yml -f docker-compose.dev.yml -f docker-compose.prod.yml config -q
      - save_cache:
          key: v1-repo-{{ .Branch }}-{{ .Revision }}
          paths:
            - .

  deploy-dev:
    <<: *defaults
    steps:
      - *restore_repo
      - run: ssh -o StrictHostKeyChecking=No -A $CARONAE_DEV_USER@$CARONAE_DEV_HOST -p $CARONAE_DEV_PORT bash -s < scripts/deploy.sh "develop"


workflows:
  version: 2

  test-and-deploy:
    jobs:
      - validate-config
      - deploy-dev
          requires:
            - validate-config
